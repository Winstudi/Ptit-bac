services:
  - type: web
    name: petit-bac
    runtime: node
    plan: free
    buildCommand: >-
      npm ci &&
      (npm install --no-save --package-lock=false sharp@0.34.1 && node optimize-assets.cjs || echo "Optimisation WebP ignorée - fallback PNG actif")
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
